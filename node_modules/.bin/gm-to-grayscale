#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gm-to-grayscale/bin/gm-to-grayscale" "$@"
  ret=$?
else 
  node  "$basedir/../gm-to-grayscale/bin/gm-to-grayscale" "$@"
  ret=$?
fi
exit $ret
